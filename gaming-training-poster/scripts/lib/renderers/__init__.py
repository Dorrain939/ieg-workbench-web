"""Renderer module entrypoints.

The legacy compose pipeline still imports lib.components. These modules expose
stable names for the platformized poster skill and allow future renderers to be
moved one-by-one without changing compose_poster_v2.py.
"""
